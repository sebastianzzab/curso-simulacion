\documentclass[12pt, a4paper]{article}

% Paquetes para codificación e idioma
\usepackage[utf8]{inputenc}
\usepackage[spanish]{babel}

% Paquetes matemáticos
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}

% Configuración de márgenes
\usepackage{geometry}
\geometry{
	left=2.5cm,
	right=2.5cm,
	top=2.5cm,
	bottom=2.5cm
}

% Paquetes para gráficos
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usepackage{float} % Para fijar la posición de la figura con [H]
\usepackage{graphicx}

% Paquetes para mostrar código
\usepackage{listings}
\usepackage{xcolor}

% Configuración de estilo para código R
\lstset{
	language=R,
	basicstyle=\small\ttfamily, % Fuente monoespaciada pequeña
	keywordstyle=\color{blue}\bfseries,
	commentstyle=\color{green!60!black},
	stringstyle=\color{orange},
	numbers=left, % Números de línea a la izquierda
	numberstyle=\tiny\color{gray},
	stepnumber=1,
	numbersep=5pt,
	backgroundcolor=\color{white},
	showspaces=false,
	showstringspaces=false,
	showtabs=false,
	frame=single, % Marco alrededor del código
	rulecolor=\color{black!30},
	tabsize=2,
	captionpos=b,
	breaklines=true, % Romper líneas largas automáticamente
	breakatwhitespace=false,
	title=\lstname,
	inputencoding=utf8,
	extendedchars=true,
	% Mapeo de caracteres especiales para que no fallen en el entorno de código
	literate={á}{{\'a}}1 {é}{{\'e}}1 {í}{{\'i}}1 {ó}{{\'o}}1 {ú}{{\'u}}1 {ñ}{{\~n}}1 {Ñ}{{\~N}}1
}



\begin{document}
	
	\begin{titlepage}
		\centering
		\normalsize % Asegura tamaño 12 en portada
		
		% --- LOGO ---
		\includegraphics[width=3cm]{logo.png} 
		
		\vspace{0.5cm}
		
		% --- ENCABEZADO INSTITUCIONAL ---
		UNIVERSIDAD DE ORIENTE \\
		NÚCLEO DE NUEVA ESPARTA \\
		ESCUELA DE INGENIERÍA Y CIENCIAS APLICADAS \\
		DEPARTAMENTO DE ESTADÍSTICA \\
		
		\vspace{3.5cm} 
		
		% --- TÍTULO ---
		\textbf{MÉTODO DE ACEPTACIÓN Y RECHAZO: EJERCICIO PROPUESTO} \\
		
		\vspace{3cm} 
		
		% --- AUTORES ---
		Presentado por: \\
		\vspace{0.5cm}
		
		Rios Luis C.I: 29.789.894 \\
		Villarroel Rosmaurys C.I: 30.209.986 \\
		Zabala Sebastian C.I: 30.277.517 \\
		Valerio Valeria C.I: 30.141.916 \\
		
		\vspace{1.5cm} 
		
		% --- INFORMACIÓN DEL CURSO ---
		Al curso de Simulación (2205393) \\
		Sección 0620 \\
		
		\vspace{0.5cm}
		
		% --- PROFESOR ---
		Prof. Oswaldo Bello \\
		
		\vfill 
		
		% --- FECHA Y LUGAR ---
		Guatamare, Diciembre de 2025
		
	\end{titlepage}
	
	\section{Planteamiento del Problema}
	
	Se desea generar valores aleatorios que sigan la función de densidad de probabilidad $f(x)$ definida por:
	
	\begin{equation}
		f(x) = 
		\begin{cases} 
			\frac{1}{16} (3x^2 + 2x + 2) & \text{si } 0 \leq x \leq 2 \\
			0 & \text{en otro caso}
		\end{cases}
	\end{equation}
	
	Para simular esta variable aleatoria, se utilizará el algoritmo de aceptación y rechazo, detallando paso a paso su construcción y analizando su eficiencia teórica.
	
	\section{Desarrollo del Método}
	
	El método de aceptación y rechazo se basa en encontrar una función de densidad auxiliar $g(x)$ fácil de simular y una constante $c$ tal que la función envolvente $c \cdot g(x)$ cubra totalmente a $f(x)$ (es decir, $c \cdot g(x) \geq f(x)$).
	
	\subsection{Selección de la función auxiliar $g(x)$}
	
	Dado que el dominio de $f(x)$ es el intervalo acotado $[0, 2]$, la elección más eficiente para la densidad instrumental es una distribución Uniforme $U(0, 2)$. Su función de densidad es:
	
	\begin{equation}
		g(x) = \frac{1}{2-0} = \frac{1}{2}, \quad \text{para } 0 \leq x \leq 2
	\end{equation}
	
	Para generar una variable aleatoria $Y$ con esta densidad a partir de un número aleatorio uniforme estándar $R \sim U(0,1)$, aplicamos la transformación:
	\[ Y = 2R \]
	
	\subsection{Cálculo de la constante de mayoración $c$}
	
	Para garantizar que la función auxiliar acote a la función objetivo, debemos encontrar el máximo de la razón entre ambas:
	
	\begin{equation}
		\frac{f(x)}{g(x)} = \frac{\frac{1}{16}(3x^2 + 2x + 2)}{\frac{1}{2}} = \frac{1}{8}(3x^2 + 2x + 2)
	\end{equation}
	
	La función $h(x) = 3x^2 + 2x + 2$ es una parábola con coeficientes positivos, lo que implica que es estrictamente creciente en el intervalo $[0, 2]$. Por lo tanto, su valor máximo se alcanza en el extremo derecho $x = 2$:
	
	\begin{equation}
		\max_{0 \leq x \leq 2} \left( \frac{f(x)}{g(x)} \right) = \frac{1}{8}(3(2)^2 + 2(2) + 2) = \frac{18}{8} = \frac{9}{4} = 2.25
	\end{equation}
	
	Por consiguiente, la constante óptima es $c = 2.25$. La función envolvente (techo de la caja de aceptación) queda definida por $c \cdot g(x) = 2.25 \cdot 0.5 = 1.125$.
	
	A continuación, se presenta la representación gráfica de la función objetivo $f(x)$ y la función envolvente utilizada.
	
	\begin{figure}[H]
		\centering
		\begin{tikzpicture}
			\begin{axis}[
				axis lines = left,
				xlabel = $x$,
				ylabel = {Densidad},
				ymin = 0, ymax = 1.3,
				xmin = -0.1, xmax = 2.2,
				domain = 0:2,
				samples = 100,
				grid = major,
				width = 12cm,
				height = 8cm,
				legend pos = south east,
				title = {Método de Aceptación y Rechazo}
				]
				
				% Gráfica de la función objetivo f(x)
				\addplot [
				domain=0:2, 
				samples=100, 
				color=blue, 
				ultra thick
				]
				{(3*x^2 + 2*x + 2)/16};
				\addlegendentry{$f(x)$}
				
				% Gráfica de la función c * g(x)
				\addplot [
				domain=0:2, 
				color=red, 
				dashed,
				thick
				]
				{1.125};
				\addlegendentry{$c \cdot g(x) = 1.125$}
				
				% Relleno del área de aceptación
				\addplot [
				draw=none,
				fill=blue!10,
				domain=0:2
				]
				{(3*x^2 + 2*x + 2)/16} \closedcycle;
				
			\end{axis}
		\end{tikzpicture}
		\caption{Visualización del método. El área sombreada en azul representa la probabilidad de aceptación, mientras que el espacio entre la curva azul y la línea roja punteada representa la zona de rechazo.}
		\label{fig:grafica_rechazo}
	\end{figure}
	
	\subsection{Criterio de Aceptación}
	
	Aceptamos el candidato $Y$ si un número aleatorio auxiliar $U \sim U(0,1)$ satisface:
	\[ U \leq \frac{f(Y)}{c \cdot g(Y)} \]
	
	Sustituyendo las expresiones:
	\begin{equation}
		U \leq \frac{\frac{1}{16}(3Y^2 + 2Y + 2)}{1.125} \implies U \leq \frac{3Y^2 + 2Y + 2}{18}
	\end{equation}
	
	Para eficiencia computacional, expresamos la condición como:
	\[ 18U \leq 3Y^2 + 2Y + 2 \]
	
	\section{Algoritmo Resultante}
	
	\begin{enumerate}
		\item \textbf{Generar:} Obtener dos números aleatorios independientes $R_1, R_2 \sim U(0, 1)$.
		\item \textbf{Transformar:} Calcular el candidato $Y = 2 R_1$ (distribuido en $[0, 2]$).
		\item \textbf{Evaluar:} Verificar si $18 R_2 \leq 3Y^2 + 2Y + 2$.
		\item \textbf{Decidir:}
		\begin{itemize}
			\item Si es \textbf{Verdadero}: Aceptar $X = Y$ y terminar.
			\item Si es \textbf{Falso}: Rechazar y volver al paso 1.
		\end{itemize}
	\end{enumerate}
	
	\section{Estudio de la Eficiencia}
	
	La eficiencia del algoritmo está determinada por la constante $c = 2.25$.
	
	\subsection{Probabilidad de Aceptación ($P_A$)}
	Es la probabilidad de que un candidato generado sea aceptado en una iteración dada:
	\begin{equation}
		P_A = \frac{1}{c} = \frac{1}{2.25} = \frac{4}{9} \approx 44.44\%
	\end{equation}
	Esto significa que el área bajo la curva $f(x)$ (área azul en la Fig. \ref{fig:grafica_rechazo}) ocupa el $44.44\%$ del área del rectángulo definido por la función envolvente.
	
	\subsection{Costo Computacional}
	El número de iteraciones necesarias para generar una variable exitosa sigue una distribución geométrica con media $c$.
	\begin{equation}
		E[\text{Intentos}] = c = 2.25
	\end{equation}
	
	En promedio, se requieren 2.25 iteraciones (generando 4.5 números aleatorios uniformes) para obtener un valor final de $X$.
	
	\section{Implementación en R}
	
	A continuación, se presenta el código desarrollado en el lenguaje de programación R para la simulación de la variable aleatoria. El script incluye la función para generar vectores de números aleatorios, el cálculo de la eficiencia empírica y la validación estadística mediante el test de Kolmogorov-Smirnov.
	
	\begin{lstlisting}[caption={Simulación por Aceptación y Rechazo en R}]
		# Generador por aceptación-rechazo
		r_densidad <- function(n) {
			c <- 2.25
			aceptados <- numeric(0)
			intentos_totales <- 0
			
			# Generamos por bloques para aprovechar la vectorización de R
			while(length(aceptados) < n) {
				faltan <- n - length(aceptados)
				# Estimamos cuantos generar para no quedarnos cortos 
				# (margen de seguridad de 20%)
				n_sim <- ceiling(faltan * c * 1.2) 
				intentos_totales <- intentos_totales + n_sim
				
				U1 <- runif(n_sim)
				U2 <- runif(n_sim)
				Y <- 2 * U2 # Variable candidata (cambié T por Y)
				
				# Vector lógico de aceptación
				condicion <- (18 * U1) <= (3*Y^2 + 2*Y + 2)
				
				# Guardamos solo los que cumplen
				aceptados <- c(aceptados, Y[condicion])
			}
			
			# Recortamos al tamaño exacto n
			resultado <- aceptados[1:n]
			attr(resultado, "intentos") <- intentos_totales
			return(resultado)
		}
		
		# Generar muestra
		set.seed(123)
		n <- 10000
		resultados <- r_densidad(n)
		intentos <- attr(resultados, "intentos")
		
		# Eficiencia
		cat("Número de valores generados:", n, "\n")
		cat("Número de intentos totales:", intentos, "\n")
		cat("Proporción de aceptación empírica:", n / intentos, "\n")
		cat("Proporción teórica (1/c):", 1/2.25, "\n")
		cat("Número medio de intentos por valor generado:", intentos / n, "\n")
		cat("Número medio teórico (c):", 2.25, "\n")
		
		# Comparar con densidad teórica
		# Nota: Asegurarse de tener definida la función f(x) previamente
		hist(resultados, breaks = 30, freq = FALSE, 
		main = "Distribución simulada vs teórica",
		xlab = "x", ylim = c(0, 1.2))
		curve(f, from = 0, to = 2, add = TRUE, col = "blue", lwd = 2)
		legend("topleft", legend = c("Simulación", "Teórica"), 
		col = c("black", "blue"), lty = c(1, 1), lwd = c(1, 2))
		
		# Test de bondad de ajuste (Kolmogorov-Smirnov)
		ks_test <- ks.test(resultados, function(x) {
			ifelse(x < 0, 0, ifelse(x > 2, 1, (x^3 + x^2 + 2*x)/16))
		})
		cat("\nTest de Kolmogorov-Smirnov:\n")
		cat("Estadístico D =", ks_test$statistic, "\n")
		cat("p-valor =", ks_test$p.value, "\n")
	\end{lstlisting}
	
\end{document}